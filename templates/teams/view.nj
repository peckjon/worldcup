{% extends '../includes/base.nj' %}

{% block content %}
    {% if team %}
        {% for player in team.players %}
        <div class="col-md-4">
            <div class="panel panel-default">
                {% include 'includes/player.nj' %}
            </div>
        </div>
        {% endfor %}
    {% else %}
        <h4>No info available for this god damned team</h4>
    {% endif %}
{% endblock %}
